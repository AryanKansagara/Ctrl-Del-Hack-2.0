# Groq API key for live conversation summarization (Who is this? → tap face to save).
# Get a key at https://console.groq.com/
# Optional: if not set, "summarize and save" will return 503.
GROQ_API_KEY=
